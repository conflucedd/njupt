{
    "version": "0.2.0",
    "configurations": [
        {
            "preLaunchTask": "cargo build",
            "name": "gdb",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/back/target/debug/back",
            "cwd": "${workspaceFolder}/back/"
        }
    ]
}